name: Update gh-aw

on:
  schedule:
    - cron: '0 9 * * 2'  # Tuesday 9am UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get current and latest versions
        id: versions
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          CURRENT=$(grep -m1 'generated by gh-aw' .github/workflows/audit-checklist.lock.yml | grep -oP 'v[\d.]+')
          LATEST=$(gh release view --repo github/gh-aw --json tagName -q '.tagName')
          echo "current=$CURRENT" >> $GITHUB_OUTPUT
          echo "latest=$LATEST" >> $GITHUB_OUTPUT
          echo "Current: $CURRENT, Latest: $LATEST"

      - name: Install gh-aw ${{ steps.versions.outputs.latest }}
        if: steps.versions.outputs.current != steps.versions.outputs.latest
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: gh extension install github/gh-aw

      - name: Recompile all workflows
        if: steps.versions.outputs.current != steps.versions.outputs.latest
        run: gh aw compile .github/workflows/audit-checklist.md

      - name: Open PR
        if: steps.versions.outputs.current != steps.versions.outputs.latest
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          CURRENT: ${{ steps.versions.outputs.current }}
          LATEST: ${{ steps.versions.outputs.latest }}
        run: |
          if [ -z "$(git diff --name-only)" ]; then
            echo "No file changes after recompile -- skipping PR."
            exit 0
          fi

          BRANCH="update-gh-aw-${LATEST}"

          if gh pr list --repo "$GITHUB_REPOSITORY" --head "$BRANCH" --json number --jq 'length' | grep -q '^[1-9]'; then
            echo "PR for $BRANCH already open -- skipping."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config --unset-all http.https://github.com/.extraheader || true
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}"
          git checkout -b "$BRANCH"
          git add .github/workflows/*.lock.yml .github/aw/
          git commit -m "Upgrade gh-aw from $CURRENT to $LATEST"
          git push --force origin "$BRANCH"

          gh pr create \
            --title "Upgrade gh-aw $CURRENT to $LATEST" \
            --base main \
            --head "$BRANCH" \
            --body "Automated recompile of workflow lock files with gh-aw $LATEST.

          ## Changes
          - Recompiled \`audit-checklist.lock.yml\`

          Review the diff to confirm no unexpected changes before merging."
